import PALETTE from 'constants/palette';

const theme = {
  red: PALETTE.RED,
  yellow: PALETTE.YELLOW,
  green: PALETTE.GREEN,
  blue: PALETTE.BLUE,
  gray: PALETTE.GRAY,
  white: PALETTE.WHITE,
};

export default theme;
